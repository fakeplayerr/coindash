extends "res://src/characters/base_npc.gd"

# Human NPCs don't need any special overrides
# They already work with the base implementation

func _ready():
	# Call the parent _ready function to initialize common properties
	super._ready()
